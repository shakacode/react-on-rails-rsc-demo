// Entry point for auto_load_bundle — re-exports the server component
// No 'use client' directive — this is a Server Component
import ProductCatalog from '../components/ProductCatalog';

export default ProductCatalog;
